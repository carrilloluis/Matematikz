\documentclass[11pt, twocolumn, landscape, a4paper]{article}

\usepackage[margin=1cm]{geometry}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage{amssymb, amsmath, amsbsy}
\usepackage{tasks}
\usepackage{enumerate}
\usepackage{graphicx}

\usepackage[pdftex]{hyperref}
\hypersetup{pdftitle={Repaso en Aritmética},pdfauthor={Luis Carrillo Gutiérrez}}

\fontfamily{cmss}\selectfont
\renewcommand{\familydefault}{\sfdefault}
\pagestyle{empty}

\begin{document}
\subsection*{Aritmética - Repaso}

\begin{itemize}
\item{El valor de verdad de las siguientes proposiciones es :
\begin{enumerate}[I.]
\item{Si $\mathrm{A} = \mathrm{B}$, entonces $\mathrm{A}\;\Delta\;\mathrm{B} = \emptyset$}
\item{Si $B \subset A \implies B \cup A = B$}
\item{Si $A = B \implies A \subset B$}
\end{enumerate}
\begin{tasks}(5)
\task{VVV}\task{VVF}\task{VFV}\task{FVV}\task{FFV}
\end{tasks}
}
\item{La suma del numerador y el denominador de una fracción es $144$. Hallar dicha fracción si al ser simplificada se obtiene $5/7$. Dar la diferencia de los términos.
\begin{tasks}(5)
\task{20}\task{24}\task{28}\task{30}\task{34}
\end{tasks}
}
\item{Sabiendo que : $\mathtt{CA.}(\overline{abc}) - \overline{cba} = 254$ y $a - c = 2$. Calcular : $abc$.
\begin{tasks}(5)
\task{10}\task{56}\task{60}\task{81}\task{90}
\end{tasks}
}
\item{Se tiene los conjuntos :
$A = \{ x \in \mathbb{N} / 3 \leq x \leq 11\}$ y \\ $B = \{ x \in \mathbb{N} / 3x \leq 5x - 2 \leq 23\}$
\begin{tasks}(3)
\task{3 elementos}
\task{4 elementos}
\task{6 elementos}
\task{8 elementos}
\task{10 elementos}
\end{tasks}
}
\item{Si $\dfrac{a + b}{ab} = 0{.}\bar{3}$, entonces el valor de : $3a^2 + b$, es :
\begin{tasks}(5)
\task{10}\task{13}\task{19}\task{23}\task{24}
\end{tasks}
}
\end{itemize}
\end{document}